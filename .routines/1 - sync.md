Clock You to Jay sync routine:
1. Update `main` from Clock You.
2. Remove finished local branches only when their work is preserved, as described in `CONTRIBUTING.md`.
3. Update branches following the documented merge flow.
4. If Jay's current version has a matching `Release <version>` commit, bump the patch version and version code once; otherwise leave them unchanged.
5. Push the updated branches to `origin`.
